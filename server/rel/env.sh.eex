#!/bin/sh

# Long names are required so libcluster / dns_cluster can discover peers by
# hostname. On Railway, each replica is reachable at its RAILWAY_PRIVATE_IP
# inside the private network.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="${RELEASE_NODE:-eir@${RAILWAY_PRIVATE_IP:-127.0.0.1}}"
